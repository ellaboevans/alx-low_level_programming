#include "main.h"

/**
  * print_numbers - prints the numbers
  * Return: 0
*/

void print_numbers(void)
{
	int num;

	for (num = 0; num < 10; num++)
	{
		_putchar(num);
	}
	_putchar('\n');

	return (0);
}
